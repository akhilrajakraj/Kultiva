"""Development settings for the professional Kultiva runtime."""
from .base import *

DEBUG = True
ALLOWED_HOSTS = [
    host.strip()
    for host in os.environ.get("DJANGO_ALLOWED_HOSTS", "localhost,127.0.0.1").split(",")
    if host.strip()
]
